@model IEnumerable<MEIAdmin.Models.Fornecedor>

@{
    ViewData["Title"] = "Lista de Fornecedores";
}

<h2>Lista de Fornecedores</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Adicionar Fornecedor</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Razão Social</th>
            <th>Contato</th>
            <th>Telefone</th>
            <th>Email</th>
            <th>Status</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var fornecedor in Model)
        {
            <tr>
                <td>@fornecedor.RazaoSocial</td>
                <td>@fornecedor.Contato</td>
                <td>@fornecedor.Telefone</td>
                <td>@fornecedor.Email</td>
                <td>
                    @(fornecedor.Ativo ? "Ativo" : "Inativo")
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@fornecedor.Id" class="btn btn-warning btn-sm">Editar</a>
                    <a asp-action="Details" asp-route-id="@fornecedor.Id" class="btn btn-info btn-sm">Detalhes</a>
                    <form asp-action="AtivarDesativar" method="post" style="display:inline;">
                        <input type="hidden" name="id" value="@fornecedor.Id" />
                        <button type="submit" class="btn @(fornecedor.Ativo ? "btn-danger" : "btn-success") btn-sm">
                            @(fornecedor.Ativo ? "Desativar" : "Ativar")
                        </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
